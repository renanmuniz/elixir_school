#tuples

#Executar esses comando no terminal interativo(iex) porque por script não está funcionando direito.

#tuples são similares a listas, porem são armazenadas sequecialmente na memoria
#mais rapido de buscar mas mais lento ao inserir/remover

#é mais utilizado em pattern matching, sera visto mais a frente



my_tuple = {:pi, 3.141516} #{:pi, 3.141516}
File.read("/home/renanmerlo/teste_elixir.txt") #{:ok, "conteudo do arquivo!"}
File.read("/home/renanmerlo/arquivo_que_nao_existe.txt") #{:error, :enoent}
